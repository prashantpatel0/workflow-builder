JWT_SECRET=supersecretjwtkey
PORT=4000
NODE_ENV=development
ORIGIN=http://localhost:5173
# Unused auth settings removed for production build
# Provide SMTP_URL or individual host/port/user/pass
# SMTP_URL=smtp://user:pass@smtp.example.com:587
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
FROM_EMAIL="Workflow Builder <no-reply@example.com>"

# Optional Mailtrap Email Sending API
# If provided, API will be used instead of SMTP.
MAILTRAP_API_TOKEN=
# MAILTRAP_API_BASE defaults to https://send.api.mailtrap.io/api
MAILTRAP_API_BASE=