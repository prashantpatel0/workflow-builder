# Server production environment sample
PORT=4000
NODE_ENV=production
# If you host client separately, set CORS origin:
# ORIGIN=https://your-frontend-domain

# Auth is currently disabled in this build. If re-enabled, add:
# JWT_SECRET=your-strong-secret
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# FROM_EMAIL="Workflow Builder <no-reply@example.com>"
